# Copy this file to .env.local and fill in the values
# Do not commit .env.local to git (it is in .gitignore)

# ============================================
# Backend API
# ============================================
# Base URL for REST API (used by auth, chat, nurse, puskesmas, etc.)
NEXT_PUBLIC_API_URL=http://localhost:8000
# Production example:
# NEXT_PUBLIC_API_URL=https://api.wellmom.id

# ============================================
# WebSocket (Chat feature)
# ============================================
# Optional. If not set, WebSocket URL is derived from NEXT_PUBLIC_API_URL
# (http -> ws, https -> wss)
NEXT_PUBLIC_WS_BASE_URL=ws://localhost:8000
# Production example:
# NEXT_PUBLIC_WS_BASE_URL=wss://api.wellmom.id
